---
// src/pages/index.astro
// xyzlab.dev - chr0nzz's app portfolio
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Self-hosted, open source developer tools by chr0nzz. Traefik Manager, Pulse, and more." />
  <meta name="gh-token" content={import.meta.env.GH_TOKEN || ''} />
  <title>xyzlab.dev</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="stylesheet" href="/styles/global.css" />
  <script is:inline>
    const t = localStorage.getItem('theme');
    if (t) document.documentElement.setAttribute('data-theme', t);
  </script>
</head>

<body>

<!-- ─── Nav ──────────────────────────────────────────────────────────────────── -->
<nav class="nav">
  <a href="/" class="nav-logo mono">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="20" height="20" style="border-radius:4px;margin-right:8px;vertical-align:-4px;flex-shrink:0">
      <rect width="32" height="32" rx="6" fill="var(--bg)"/>
      <rect x="6" y="10" width="8" height="3" rx="1" fill="#22c55e"/>
      <rect x="6" y="16" width="14" height="3" rx="1" fill="#22c55e" opacity="0.5"/>
      <rect x="6" y="22" width="18" height="3" rx="1" fill="#22c55e" opacity="0.25"/>
    </svg>xyz<span class="accent">lab</span>.dev</a>
  <div class="nav-links">
    <a href="#apps">apps</a>
    <a href="#activity">activity</a>
    <a href="#issues">issues</a>
    <a href="#discussions">discussions</a>
    <button class="theme-toggle" id="theme-toggle" aria-label="Toggle theme">
      <svg class="icon-sun" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
        <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
        <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
        <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
      </svg>
      <svg class="icon-moon" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
      </svg>
    </button>
    <a
      href="https://github.com/chr0nzz"
      target="_blank"
      rel="noopener"
      class="nav-badge mono"
    >
      chr0nzz ↗
    </a>
  </div>
</nav>

<!-- ─── Hero ─────────────────────────────────────────────────────────────────── -->
<section class="hero">
  <div class="hero-tag mono">// self-hosted. open source. no nonsense.</div>

  <h1 class="hero-title">
    apps built for people<br />
    who <strong>run their own stuff</strong>
  </h1>

  <p class="hero-sub">
    Developer tools for your home server. Docker-native, open source, no cloud required.
  </p>

  <div class="terminal" id="hero-terminal" tabindex="0">
    <div class="terminal-bar">
      <div class="dot r"></div>
      <div class="dot y"></div>
      <div class="dot g"></div>
      <span class="terminal-title mono">bash - xyzlab.dev</span>
    </div>
    <div class="term-body" id="term-body" dir="ltr">
      <div class="term-input-row">
        <span class="term-accent">$</span>
        <span class="term-typed mono" id="term-typed"></span>
        <span class="cursor" id="term-cursor"></span>
      </div>
    </div>
    <input id="term-input" type="text" autocomplete="off" autocorrect="off" autocapitalize="none" spellcheck="false" dir="ltr" style="position:fixed;left:-9999px;top:50%;width:200px;height:40px;font-size:16px;opacity:0" />
  </div>

  <div class="hero-ctas">
    <a href="#apps" class="btn-primary">browse apps</a>
    <a href="https://github.com/chr0nzz" target="_blank" rel="noopener" class="btn-secondary mono">
      github.com/chr0nzz ↗
    </a>
  </div>
</section>

<!-- ─── Stats Bar ─────────────────────────────────────────────────────────────── -->
<div class="stats-row">
  <div class="stat">
    <div class="stat-val mono">5<span class="accent">+</span></div>
    <div class="stat-label">open source apps</div>
  </div>
  <div class="stat">
    <div class="stat-val mono"><span data-stat="stars">…</span><span class="accent">★</span></div>
    <div class="stat-label">total github stars</div>
  </div>
  <div class="stat">
    <div class="stat-val mono"><span data-stat="forks">…</span></div>
    <div class="stat-label">forks</div>
  </div>
  <div class="stat">
    <div class="stat-val mono" id="contrib-count">…</div>
    <div class="stat-label">contributions this year</div>
  </div>
</div>

<!-- ─── Apps ─────────────────────────────────────────────────────────────────── -->
<section class="section" id="apps">
  <div class="section-header">
    <span class="section-title mono">// apps</span>
    <div class="section-line"></div>
    <span class="section-count mono">4 projects</span>
  </div>

  <div class="apps-grid">

    <!-- Traefik Manager (featured, wide) -->
    <div class="app-card featured wide-card">
      <div class="app-top">
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/icon.png" class="app-icon-img" alt="Traefik Manager icon" />
        <div class="app-badges" data-repo-badges="traefik-manager">
          <span class="badge green">stable</span>
        </div>
      </div>
      <div class="app-name">Traefik Manager</div>
      <div class="app-desc">
        Web UI for managing your Traefik instance. Live dashboard, TLS certs, access logs, plugins - all from your browser.
      </div>
      <div class="app-features" style="grid-template-columns: repeat(3, 1fr)">
        <div class="feat"><div class="feat-dot"></div>live dashboard</div>
        <div class="feat"><div class="feat-dot"></div>TLS cert viewer</div>
        <div class="feat"><div class="feat-dot"></div>access log stream</div>
        <div class="feat"><div class="feat-dot"></div>plugin viewer</div>
        <div class="feat"><div class="feat-dot"></div>auto backups</div>
        <div class="feat"><div class="feat-dot"></div>OIDC / SSO</div>
      </div>
      <div class="app-screenshots">
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-dashboard.png" data-dark="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-dashboard.png" data-light="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/light-dashboard.png" class="screenshot theme-screenshot" alt="Dashboard" loading="lazy" />
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-routes-cards.png" data-dark="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-routes-cards.png" data-light="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/light-routes-cards.png" class="screenshot theme-screenshot" alt="Routes" loading="lazy" />
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-middlewares-cards.png" data-dark="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-middlewares-cards.png" data-light="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/light-middlewares-cards.png" class="screenshot theme-screenshot" alt="Middlewares" loading="lazy" />
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-services-cards.png" data-dark="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-services-cards.png" data-light="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/light-services-cards.png" class="screenshot theme-screenshot" alt="Services" loading="lazy" />
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-route-map.png" data-dark="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/dark-route-map.png" data-light="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/light-route-map.png" class="screenshot theme-screenshot" alt="Route map" loading="lazy" />
      </div>
      <div class="app-footer">
        <span class="app-stat mono"><strong data-repo-stars="traefik-manager">…</strong> ★</span>
        <div class="app-links">
          <a href="https://traefik-manager.xyzlab.dev" target="_blank" rel="noopener" class="app-link mono">docs ↗</a>
          <a href="https://github.com/chr0nzz/traefik-manager" target="_blank" rel="noopener" class="app-link mono">github ↗</a>
        </div>
      </div>
    </div>

    <!-- Traefik Manager Mobile -->
    <div class="app-card">
      <div class="app-top">
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/icon.png" class="app-icon-img" alt="Traefik Manager Mobile icon" />
        <div class="app-badges" data-repo-badges="traefik-manager-mobile">
          <span class="badge green">stable</span>
        </div>
      </div>
      <div class="app-name">Traefik Manager Mobile</div>
      <div class="app-desc">
        React Native companion app for Traefik Manager. Browse routes, middlewares, and services from your phone.
      </div>
      <div class="app-features">
        <div class="feat"><div class="feat-dot"></div>browse routes</div>
        <div class="feat"><div class="feat-dot"></div>enable / disable</div>
        <div class="feat"><div class="feat-dot"></div>middleware editor</div>
        <div class="feat"><div class="feat-dot"></div>dark / light theme</div>
      </div>
      <div class="app-footer">
        <span class="app-stat mono"><strong data-repo-stars="traefik-manager-mobile">…</strong> ★</span>
        <div class="app-links">
          <a href="https://github.com/chr0nzz/traefik-manager-mobile" target="_blank" rel="noopener" class="app-link mono">github ↗</a>
          <a href="https://play.google.com/store/apps/details?id=dev.chr0nzz.traefikmanager" target="_blank" rel="noopener" class="app-link mono">
            <svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor" style="margin-right:4px;flex-shrink:0"><path d="M3 20.5v-17c0-.83 1-.83 1.5-.5l15 8.5-15 8.5c-.5.33-1.5.33-1.5-.5z"/></svg>play store ↗
          </a>
        </div>
      </div>
    </div>

    <!-- traefik-stack -->
    <div class="app-card">
      <div class="app-top">
        <img src="https://github.com/chr0nzz/traefik-manager/raw/main/docs/public/images/icon.png" class="app-icon-img" alt="traefik-stack icon" />
        <div class="app-badges" data-repo-badges="traefik-stack">
          <span class="badge gray">bash</span>
        </div>
      </div>
      <div class="app-name">traefik-stack</div>
      <div class="app-desc">
        Traefik and Traefik Manager installer - Docker and native Linux install.
      </div>
      <div class="app-features">
        <div class="feat"><div class="feat-dot"></div>one command</div>
        <div class="feat"><div class="feat-dot"></div>interactive setup</div>
        <div class="feat"><div class="feat-dot"></div>docker compose</div>
        <div class="feat"><div class="feat-dot"></div>native linux</div>
      </div>
      <div class="install-block">
        <code class="install-cmd mono" id="traefik-stack-cmd">curl -fsSL https://get-traefik.xyzlab.dev | bash</code>
        <button class="copy-btn mono" onclick="(async(b)=>{const t='curl -fsSL https://get-traefik.xyzlab.dev | bash';try{await navigator.clipboard.writeText(t)}catch(e){const i=document.createElement('textarea');i.value=t;document.body.appendChild(i);i.select();document.execCommand('copy');i.remove()}b.textContent='copied';setTimeout(()=>b.textContent='copy',1500)})(this)">copy</button>
      </div>
      <div class="app-footer">
        <span class="app-stat mono"><strong data-repo-stars="traefik-stack">…</strong> ★</span>
        <div class="app-links">
          <a href="https://github.com/chr0nzz/traefik-stack" target="_blank" rel="noopener" class="app-link mono">github ↗</a>
        </div>
      </div>
    </div>

    <!-- ntfy-adapter -->
    <div class="app-card">
      <div class="app-top">
        <img src="/icons/ntfy-adapter.svg" class="app-icon-img" alt="ntfy-adapter icon" />
        <div class="app-badges" data-repo-badges="ntfy-adapter">
          <span class="badge green">stable</span>
        </div>
      </div>
      <div class="app-name">ntfy-adapter</div>
      <div class="app-desc">
        Docker adapter bridging ntfy notifications into Homepage's customapi widget as a live dynamic list.
      </div>
      <div class="app-features">
        <div class="feat"><div class="feat-dot"></div>ntfy integration</div>
        <div class="feat"><div class="feat-dot"></div>homepage widget</div>
        <div class="feat"><div class="feat-dot"></div>priority expiry</div>
        <div class="feat"><div class="feat-dot"></div>docker native</div>
      </div>
      <div class="app-footer">
        <span class="app-stat mono"><strong data-repo-stars="ntfy-adapter">…</strong> ★</span>
        <div class="app-links">
          <a href="https://github.com/chr0nzz/ntfy-adapter" target="_blank" rel="noopener" class="app-link mono">github ↗</a>
        </div>
      </div>
    </div>

    <!-- jellyfin-widget-proxy -->
    <div class="app-card">
      <div class="app-top">
        <img src="/icons/jellyfin-widget-proxy.svg" class="app-icon-img" alt="jellyfin-widget-proxy icon" />
        <div class="app-badges" data-repo-badges="jellyfin-widget-proxy">
          <span class="badge green">stable</span>
        </div>
      </div>
      <div class="app-name">jellyfin-widget-proxy</div>
      <div class="app-desc">
        Lightweight proxy server that fetches Jellyfin data for use in dashboards and Homepage widgets.
      </div>
      <div class="app-features">
        <div class="feat"><div class="feat-dot"></div>jellyfin API</div>
        <div class="feat"><div class="feat-dot"></div>homepage widget</div>
        <div class="feat"><div class="feat-dot"></div>lightweight</div>
        <div class="feat"><div class="feat-dot"></div>docker native</div>
      </div>
      <div class="app-footer">
        <span class="app-stat mono"><strong data-repo-stars="jellyfin-widget-proxy">…</strong> ★</span>
        <div class="app-links">
          <a href="https://github.com/chr0nzz/jellyfin-widget-proxy" target="_blank" rel="noopener" class="app-link mono">github ↗</a>
        </div>
      </div>
    </div>

  </div>
</section>

<!-- ─── Activity ──────────────────────────────────────────────────────────────── -->
<section class="section" id="activity">
  <div class="section-header">
    <span class="section-title mono">// commit activity</span>
    <div class="section-line"></div>
    <span class="section-count mono" id="contrib-count-inline">loading…</span>
  </div>

  <!-- Contribution graph injected by github.js -->
  <div id="contrib-graph" style="min-height: 120px">
    <div class="skeleton" style="height: 104px; width: 100%"></div>
  </div>

  <!-- Recent commits -->
  <div style="margin-top: 28px">
    <div class="section-header" style="margin-bottom: 12px">
      <span class="section-title mono">// recent commits</span>
      <div class="section-line"></div>
    </div>
    <div class="term-window">
      <div class="term-bar">
        <span class="term-bar-cmd mono">git log --all --oneline</span>
      </div>
      <div id="commits-list" class="commits-list">
        {Array.from({ length: 4 }).map(() => (
          <div class="commit-item">
            <span class="commit-prompt mono">$</span>
            <div class="skeleton" style="width: 60px; height: 13px; flex-shrink: 0; margin-top: 1px"></div>
            <div class="skeleton" style="width: 110px; height: 13px; flex-shrink: 0; margin-top: 1px"></div>
            <div class="skeleton" style="flex: 1; height: 13px; margin-top: 1px"></div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<!-- ─── Issues ────────────────────────────────────────────────────────────────── -->
<section class="section" id="issues">
  <div class="section-header" style="margin-bottom: 14px">
    <span class="section-title mono">// open issues</span>
    <div class="section-line"></div>
  </div>

  <div class="issues-wrap">
    <div class="issues-header">
      <span class="issues-tab active mono" id="issues-open-tab">
        <svg width="14" height="14" viewBox="0 0 16 16" fill="currentColor" style="vertical-align:-2px;margin-right:4px"><path d="M8 9.5a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Z"/><path d="M8 0a8 8 0 1 1 0 16A8 8 0 0 1 8 0ZM1.5 8a6.5 6.5 0 1 0 13 0 6.5 6.5 0 0 0-13 0Z"/></svg>
        <span id="issues-count">0</span>&nbsp;Open
      </span>
    </div>
    <div id="issues-grid" class="issues-grid">
      {Array.from({ length: 3 }).map(() => (
        <div class="issue-item">
          <div style="width:16px;height:16px;flex-shrink:0" class="skeleton"></div>
          <div class="issue-body">
            <div class="skeleton" style="width:60%;height:14px;margin-bottom:6px"></div>
            <div class="skeleton" style="width:40%;height:11px"></div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ─── Discussions ───────────────────────────────────────────────────────────── -->
<section class="section" id="discussions">
  <div class="section-header" style="margin-bottom: 14px">
    <span class="section-title mono">// discussions</span>
    <div class="section-line"></div>
  </div>

  <div class="issues-wrap">
    <div class="issues-header">
      <span class="issues-tab active mono">
        <svg width="14" height="14" viewBox="0 0 16 16" fill="currentColor" style="vertical-align:-2px;margin-right:4px"><path d="M1.75 1h8.5c.966 0 1.75.784 1.75 1.75v5.5A1.75 1.75 0 0 1 10.25 10H7.061l-2.574 2.561A1.75 1.75 0 0 1 1.5 11.25V1.75C1.5 1.784 1.75 1 1.75 1ZM1 1.75v9.5c0 .138.112.25.25.25.068 0 .135-.028.183-.076l2.75-2.673A.25.25 0 0 1 4.363 8.5H10.25a.25.25 0 0 0 .25-.25v-5.5a.25.25 0 0 0-.25-.25h-8.5a.25.25 0 0 0-.25.25Zm13 2.75v5.5a.25.25 0 0 1-.25.25h-.5a.75.75 0 0 0 0 1.5h.5c.966 0 1.75-.784 1.75-1.75v-5.5a.75.75 0 0 0-1.5 0Z"/></svg>
        <span id="discussions-count">0</span>&nbsp;Discussions
      </span>
    </div>
    <div id="discussions-grid" class="issues-grid">
      {Array.from({ length: 3 }).map(() => (
        <div class="issue-item">
          <div style="width:16px;height:16px;flex-shrink:0" class="skeleton"></div>
          <div class="issue-body">
            <div class="skeleton" style="width:60%;height:14px;margin-bottom:6px"></div>
            <div class="skeleton" style="width:40%;height:11px"></div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ─── Footer ────────────────────────────────────────────────────────────────── -->
<div id="lightbox" class="lightbox">
  <div class="lightbox-backdrop"></div>
  <button class="lightbox-close">✕</button>
  <button class="lightbox-prev">‹</button>
  <button class="lightbox-next">›</button>
  <img id="lightbox-img" class="lightbox-img" src="" alt="" />
</div>

<footer class="footer">
  <div class="footer-left mono">xyz<span class="accent">lab</span>.dev - built by chr0nzz</div>
  <div class="footer-right">runs on cloudflare pages · open source</div>
</footer>

</body>
</html>

<style>
  /* Layout */
  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 28px;
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    background: color-mix(in srgb, var(--bg) 85%, transparent);
    backdrop-filter: blur(12px);
    z-index: 10;
  }
  .nav-logo { font-size: 14px; font-weight: 700; }
  .nav-links { display: flex; gap: 20px; align-items: center; }
  .nav-links a { font-size: 13px; color: var(--text-secondary); transition: color 0.15s; }
  .nav-links a:hover { color: var(--text); }
  .nav-badge {
    font-size: 11px;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    padding: 4px 10px;
    border-radius: var(--radius-sm);
    color: var(--text-secondary);
    transition: border-color 0.15s, color 0.15s;
  }
  .nav-badge:hover { border-color: var(--border-hover); color: var(--text); }
  .theme-toggle {
    background: none; border: 1px solid var(--border); border-radius: var(--radius-sm);
    color: var(--text-secondary); padding: 5px 8px; display: flex; align-items: center;
    transition: border-color 0.15s, color 0.15s; cursor: pointer;
  }
  .theme-toggle:hover { border-color: var(--border-hover); color: var(--text); }
  .icon-moon { display: none; }
  [data-theme="light"] .icon-sun { display: none; }
  [data-theme="light"] .icon-moon { display: block; }

  /* Hero */
  .hero { padding: 56px 28px 44px; border-bottom: 1px solid var(--border); }
  .hero-tag { font-size: 11px; color: var(--accent); letter-spacing: 0.08em; margin-bottom: 14px; }
  .hero-title { font-size: clamp(28px, 4vw, 42px); font-weight: 300; line-height: 1.15; margin-bottom: 16px; letter-spacing: -0.02em; }
  .hero-title strong { font-weight: 600; }
  .hero-sub { font-size: 15px; color: var(--text-secondary); max-width: 420px; line-height: 1.6; margin-bottom: 28px; }

  /* Terminal */
  .terminal {
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 14px 18px;
    width: 100%;
    margin-bottom: 28px;
    cursor: text;
    transition: border-color 0.15s;
  }
  .terminal:focus-within { border-color: var(--border-hover); }
  .terminal-bar { display: flex; gap: 6px; margin-bottom: 12px; align-items: center; }
  .dot { width: 10px; height: 10px; border-radius: 50%; }
  .dot.r { background: #ef4444; }
  .dot.y { background: #f59e0b; }
  .dot.g { background: #22c55e; }
  .terminal-title { font-size: 11px; color: var(--text-tertiary); margin-left: auto; }
  .term-body { height: 320px; overflow-y: auto; display: flex; flex-direction: column; }
  .term-input-row { display: flex; align-items: center; gap: 6px; font-family: var(--font-mono); font-size: 12.5px; line-height: 1.9; }
  .term-typed { color: var(--text); }
  :global(.term-line) { font-family: var(--font-mono); font-size: 12.5px; line-height: 1.9; white-space: pre-wrap; word-break: break-all; }
  :global(.term-dim)    { color: var(--text-tertiary); }
  :global(.term-success){ color: var(--accent); }
  :global(.term-warn)   { color: #fbbf24; }
  :global(.term-cyan)   { color: #60a5fa; }
  :global(.term-accent) { color: var(--accent); }
  :global(.term-bold)   { font-weight: 700; }
  :global(.term-prompt-inline) { color: var(--text-secondary); }
  .cursor {
    display: inline-block; width: 8px; height: 14px;
    background: var(--accent); vertical-align: middle;
    animation: blink 1.1s step-end infinite;
  }
  @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }

  /* CTAs */
  .hero-ctas { display: flex; gap: 10px; flex-wrap: wrap; }
  .btn-primary {
    font-family: var(--font-mono); font-size: 12px;
    background: var(--accent); color: #052e16;
    padding: 10px 20px; border-radius: var(--radius-md);
    font-weight: 700; border: none; transition: opacity 0.15s;
  }
  .btn-primary:hover { opacity: 0.88; }
  .btn-secondary {
    font-family: var(--font-mono); font-size: 12px;
    background: transparent; color: var(--text-secondary);
    padding: 10px 20px; border-radius: var(--radius-md);
    border: 1px solid var(--border); transition: border-color 0.15s, color 0.15s;
  }
  .btn-secondary:hover { border-color: var(--border-hover); color: var(--text); }

  /* Stats row */
  .stats-row { display: grid; grid-template-columns: repeat(4, 1fr); border-bottom: 1px solid var(--border); }
  .stat { padding: 20px 24px; border-right: 1px solid var(--border); }
  .stat:last-child { border-right: none; }
  .stat-val { font-size: 22px; font-weight: 700; color: var(--text); }
  .stat-label { font-size: 12px; color: var(--text-tertiary); margin-top: 2px; }

  /* Sections */
  .section { padding: 40px 28px; border-bottom: 1px solid var(--border); }
  .section-header { display: flex; align-items: center; gap: 10px; margin-bottom: 24px; }
  .section-title { font-size: 13px; font-weight: 500; letter-spacing: 0.04em; white-space: nowrap; }
  .section-count { font-size: 11px; color: var(--text-tertiary); white-space: nowrap; }
  .section-line { flex: 1; height: 1px; background: var(--border); }

  /* App cards */
  .apps-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
  .app-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 20px;
    transition: border-color 0.2s;
    min-width: 0;
  }
  .app-card:hover { border-color: var(--border-hover); }
  .app-card.featured { border-color: var(--accent-border); }
  .app-card.featured:hover { border-color: rgba(34,197,94,0.6); }
  .wide-card { grid-column: span 2; }

  .app-top { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 14px; }
  .app-icon {
    width: 40px; height: 40px; border-radius: 10px;
    background: var(--bg-tertiary); border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center; font-size: 18px;
  }
  .app-icon-img {
    width: 40px; height: 40px; border-radius: 10px;
    object-fit: cover; border: 1px solid var(--border);
  }
  .app-badges { display: flex; gap: 6px; flex-wrap: wrap; justify-content: flex-end; }
  .badge { font-family: var(--font-mono); font-size: 10px; padding: 3px 8px; border-radius: 4px; font-weight: 500; }
  .badge.green { background: var(--accent-dim); color: var(--accent); border: 1px solid var(--accent-border); }
  .badge.blue { background: rgba(59,130,246,0.1); color: #60a5fa; border: 1px solid rgba(59,130,246,0.25); }
  .badge.amber { background: rgba(245,158,11,0.1); color: #fbbf24; border: 1px solid rgba(245,158,11,0.25); }
  .badge.gray { background: var(--bg-tertiary); color: var(--text-secondary); border: 1px solid var(--border); }
  :global(.badge.version) { font-family: var(--font-mono); font-size: 10px; padding: 3px 8px; border-radius: 4px; font-weight: 500; background: var(--accent-dim); color: var(--accent); border: 1px solid var(--accent-border); }
  :global(.badge.prerelease) { font-family: var(--font-mono); font-size: 10px; padding: 3px 8px; border-radius: 4px; font-weight: 500; background: rgba(245,158,11,0.1); color: #fbbf24; border: 1px solid rgba(245,158,11,0.25); }

  .app-name { font-size: 15px; font-weight: 600; margin-bottom: 6px; }
  .app-desc { font-size: 13px; color: var(--text-secondary); line-height: 1.55; margin-bottom: 16px; }

  .app-features { display: grid; grid-template-columns: 1fr 1fr; gap: 7px; margin-bottom: 16px; }
  .feat { display: flex; align-items: center; gap: 7px; font-size: 12px; color: var(--text-secondary); }
  .feat-dot { width: 5px; height: 5px; border-radius: 50%; background: var(--accent); flex-shrink: 0; }

  .app-screenshots {
    display: flex; gap: 8px; overflow-x: auto; margin-top: 14px;
    padding-bottom: 4px;
    scrollbar-width: thin;
    scrollbar-color: rgba(255,255,255,0.1) transparent;
  }
  .screenshot {
    height: 90px; width: auto; flex-shrink: 0;
    border-radius: 6px; border: 1px solid var(--border);
    opacity: 0.75; transition: opacity 0.2s, border-color 0.2s, transform 0.2s;
    object-fit: cover; cursor: zoom-in;
  }
  .screenshot:hover { opacity: 1; border-color: var(--accent); transform: translateY(-2px); }
  .app-footer {
    display: flex; align-items: center; gap: 12px;
    padding-top: 14px; border-top: 1px solid var(--border);
  }
  .app-stat { font-family: var(--font-mono); font-size: 11px; color: var(--text-tertiary); }
  .app-stat strong { color: var(--text-secondary); font-weight: 500; }
  .app-links { margin-left: auto; display: flex; align-items: center; gap: 8px; }
  .app-link {
    font-size: 11px; color: var(--text-secondary); padding: 4px 10px;
    border-radius: var(--radius-sm); border: 1px solid var(--border);
    transition: border-color 0.15s, color 0.15s;
  }
  .app-link:hover { border-color: var(--border-hover); color: var(--text); }

  .install-block {
    display: flex; align-items: center; gap: 8px;
    background: var(--bg-tertiary); border: 1px solid var(--border);
    border-radius: 6px; padding: 8px 12px; margin-top: 12px;
  }
  .install-cmd { font-size: 12px; color: var(--accent); flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .copy-btn {
    font-size: 10px; padding: 3px 8px; border-radius: 4px; flex-shrink: 0;
    background: none; border: 1px solid var(--border); color: var(--text-secondary);
    cursor: pointer; transition: border-color 0.15s, color 0.15s;
  }
  .copy-btn:hover { border-color: var(--border-hover); color: var(--text); }

  .inline-code {
    font-family: var(--font-mono); font-size: 11px;
    background: var(--bg-tertiary); border: 1px solid var(--border);
    padding: 1px 6px; border-radius: 4px; color: var(--accent);
  }

  /* Commits */
  .term-window {
    background: var(--bg-secondary); border: 1px solid var(--border);
    border-radius: 8px; overflow: hidden;
  }
  .term-bar {
    display: flex; align-items: center; gap: 6px;
    padding: 9px 14px; background: var(--bg-tertiary);
    border-bottom: 1px solid var(--border);
  }
  .term-bar-cmd { font-size: 11px; color: var(--text-tertiary); }
  :global(.commits-list) { display: flex; flex-direction: column; padding: 6px 0; }
  :global(.commit-item) {
    display: flex; align-items: baseline; gap: 10px;
    padding: 7px 14px; text-decoration: none; color: inherit;
    transition: background 0.12s;
    font-family: var(--font-mono); font-size: 12px;
  }
  :global(.commit-item:hover) { background: var(--bg-tertiary); }
  :global(.commit-prompt) { color: var(--accent); flex-shrink: 0; user-select: none; }
  :global(.commit-sha) { color: var(--accent); flex-shrink: 0; opacity: 0.8; }
  :global(.commit-repo) { color: #60a5fa; flex-shrink: 0; }
  :global(.commit-msg) { color: var(--text); flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  :global(.commit-time) { color: var(--text-tertiary); flex-shrink: 0; margin-left: auto; }

  /* Issues */
  .issues-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .issues-wrap { border: 1px solid var(--border); border-radius: var(--radius-lg); overflow: hidden; }
  .issues-header {
    display: flex; align-items: center; gap: 4px;
    padding: 10px 16px; background: var(--bg-tertiary);
    border-bottom: 1px solid var(--border);
  }
  .issues-tab { font-size: 13px; color: var(--text-secondary); padding: 4px 8px; border-radius: var(--radius-sm); display: flex; align-items: center; }
  .issues-tab.active { font-weight: 600; color: var(--text); }
  .issues-grid { display: flex; flex-direction: column; }
  .issue-item {
    display: flex; align-items: flex-start; gap: 12px;
    padding: 14px 16px; border-bottom: 1px solid var(--border);
    text-decoration: none; color: inherit;
    transition: background 0.12s;
    background: var(--bg-secondary);
  }
  .issue-item:last-child { border-bottom: none; }
  .issue-item:hover { background: var(--bg-tertiary); }
  .issue-icon { flex-shrink: 0; margin-top: 2px; color: var(--accent); }
  .issue-icon.bug { color: #f87171; }
  .issue-icon.help { color: #60a5fa; }
  .issue-body { flex: 1; min-width: 0; }
  .issue-top { display: flex; align-items: center; gap: 7px; margin-bottom: 4px; flex-wrap: wrap; }
  .issue-title { font-size: 14px; font-weight: 600; color: var(--text); }
  .issue-title:hover { color: var(--accent); }
  .issue-num { font-family: var(--font-mono); font-size: 11px; color: var(--text-tertiary); flex-shrink: 0; }
  .issue-label { font-size: 11px; padding: 2px 8px; border-radius: 99px; font-weight: 500; flex-shrink: 0; }
  .issue-label.bug { background: rgba(239,68,68,0.12); color: #f87171; border: 1px solid rgba(239,68,68,0.25); }
  .issue-label.feat { background: var(--accent-dim); color: var(--accent); border: 1px solid var(--accent-border); }
  .issue-label.help { background: rgba(59,130,246,0.12); color: #60a5fa; border: 1px solid rgba(59,130,246,0.25); }
  .issue-label.default { background: var(--bg-tertiary); color: var(--text-secondary); border: 1px solid var(--border); }
  .issue-repo-tag { font-family: var(--font-mono); font-size: 11px; color: var(--text-tertiary); flex-shrink: 0; margin-left: auto; }
  .issue-meta { font-size: 12px; color: var(--text-tertiary); }

  /* Footer */
  .footer { padding: 22px 28px; display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 8px; }
  .footer-left { font-size: 12px; color: var(--text-tertiary); }
  .footer-right { font-size: 12px; color: var(--text-tertiary); }

  /* Accent color helper */
  .accent { color: var(--accent); }

  /* Responsive */
  @media (max-width: 600px) {
    .stats-row { grid-template-columns: 1fr 1fr; }
    .stat:nth-child(2) { border-right: none; }
    .issues-grid { grid-template-columns: 1fr; }
    .nav-links a:not(.nav-badge) { display: none; }

    .terminal { padding: 10px 12px; }
    .term-body { height: 220px; }
    :global(.term-line) { font-size: 11px; line-height: 1.7; }
    .term-input-row { font-size: 11px; }

    .apps-grid { grid-template-columns: 1fr; gap: 10px; }
    .wide-card { grid-column: span 1; }
    .app-card { padding: 12px; box-sizing: border-box; overflow: hidden; }

    .app-top { flex-direction: row; align-items: center; gap: 10px; margin-bottom: 8px; flex-wrap: nowrap; }
    .app-icon-img, .app-icon { width: 32px; height: 32px; flex-shrink: 0; }
    .app-badges { justify-content: flex-start; flex-wrap: wrap; gap: 3px; flex: 1; min-width: 0; }
    .badge { font-size: 9px; padding: 2px 5px; }
    :global(.badge.version) { font-size: 9px !important; padding: 2px 5px !important; }
    :global(.badge.prerelease) { font-size: 9px !important; padding: 2px 5px !important; }

    .app-name { font-size: 13px; margin-bottom: 3px; }
    .app-desc { font-size: 12px; line-height: 1.5; margin-bottom: 10px;
      display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
    .app-features { display: none; }
    .app-screenshots { display: none; }
    .install-block { margin-top: 8px; padding: 6px 8px; }
    .install-cmd { font-size: 10px; }
    .copy-btn { font-size: 9px; padding: 2px 6px; }
    .app-footer { padding-top: 8px; gap: 6px; }
    .app-stat { font-size: 10px; }
    .app-links { gap: 5px; }
    .app-link { font-size: 10px; padding: 3px 7px; }

    .lightbox-prev { left: 8px; padding: 6px 10px; font-size: 22px; }
    .lightbox-next { right: 8px; padding: 6px 10px; font-size: 22px; }
  }

  .lightbox {
    position: fixed; inset: 0; z-index: 1000;
    display: flex; align-items: center; justify-content: center;
    opacity: 0; pointer-events: none;
    transition: opacity 0.2s;
  }
  .lightbox.open { opacity: 1; pointer-events: all; }
  .lightbox-backdrop {
    position: absolute; inset: 0;
    background: rgba(0,0,0,0.85); backdrop-filter: blur(6px);
  }
  .lightbox-img {
    position: relative; z-index: 1;
    max-width: 90vw; max-height: 85vh;
    border-radius: 10px; border: 1px solid var(--border-hover);
    box-shadow: 0 32px 80px rgba(0,0,0,0.6);
    transform: scale(0.95); transition: transform 0.2s;
    object-fit: contain;
  }
  .lightbox.open .lightbox-img { transform: scale(1); }
  .lightbox-close {
    position: absolute; top: 20px; right: 24px; z-index: 2;
    background: none; border: none; color: var(--text-secondary);
    font-size: 22px; cursor: pointer; padding: 6px 10px;
    transition: color 0.15s;
  }
  .lightbox-close:hover { color: var(--text); }
  .lightbox-prev, .lightbox-next {
    position: absolute; z-index: 2; top: 50%; transform: translateY(-50%);
    background: var(--bg-secondary); border: 1px solid var(--border);
    color: var(--text); font-size: 28px; cursor: pointer;
    padding: 8px 14px; border-radius: 8px;
    transition: border-color 0.15s, color 0.15s;
  }
  .lightbox-prev { left: 20px; }
  .lightbox-next { right: 20px; }
  .lightbox-prev:hover, .lightbox-next:hover { border-color: var(--border-hover); }
</style>

<script is:inline>
  (function() {
    const saved = localStorage.getItem('theme');
    if (saved) document.documentElement.setAttribute('data-theme', saved);
    document.addEventListener('DOMContentLoaded', () => {
      const btn = document.getElementById('theme-toggle');
      if (!btn) return;
      const initial = document.documentElement.getAttribute('data-theme');
      if (initial === 'light') {
        document.querySelectorAll('.theme-screenshot').forEach(img => {
          if (img.dataset.light) img.src = img.dataset.light;
        });
      }
      btn.addEventListener('click', () => {
        const current = document.documentElement.getAttribute('data-theme');
        const next = current === 'light' ? 'dark' : 'light';
        document.documentElement.setAttribute('data-theme', next);
        localStorage.setItem('theme', next);
        document.querySelectorAll('.theme-screenshot').forEach(img => {
          img.src = img.dataset[next] || img.src;
        });
      });
    });
  })();
</script>
<script src="/scripts/terminal.js" is:inline></script>
<script src="/scripts/github.js" is:inline></script>
<script is:inline>
  (function() {
    const lb = document.getElementById('lightbox');
    const lbImg = document.getElementById('lightbox-img');
    let images = [];
    let current = 0;

    function open(imgs, idx) {
      images = imgs;
      current = idx;
      lbImg.src = images[current];
      lb.classList.add('open');
      document.body.style.overflow = 'hidden';
    }

    function close() {
      lb.classList.remove('open');
      document.body.style.overflow = '';
      setTimeout(() => { lbImg.src = ''; }, 200);
    }

    function navigate(dir) {
      current = (current + dir + images.length) % images.length;
      lbImg.style.opacity = '0';
      setTimeout(() => {
        lbImg.src = images[current];
        lbImg.style.opacity = '1';
      }, 120);
    }

    document.querySelector('.lightbox-backdrop').addEventListener('click', close);
    document.querySelector('.lightbox-close').addEventListener('click', close);
    document.querySelector('.lightbox-prev').addEventListener('click', () => navigate(-1));
    document.querySelector('.lightbox-next').addEventListener('click', () => navigate(1));
    document.addEventListener('keydown', e => {
      if (!lb.classList.contains('open')) return;
      if (e.key === 'Escape') close();
      if (e.key === 'ArrowLeft') navigate(-1);
      if (e.key === 'ArrowRight') navigate(1);
    });

    document.addEventListener('click', e => {
      const shot = e.target.closest('.screenshot');
      if (!shot) return;
      const strip = shot.closest('.app-screenshots');
      const all = [...strip.querySelectorAll('.screenshot')].map(i => i.src);
      open(all, [...strip.querySelectorAll('.screenshot')].indexOf(shot));
    });

    lbImg.style.transition = 'transform 0.2s, opacity 0.12s';
  })();
</script>